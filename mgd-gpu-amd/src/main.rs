//! mgd-gpu-amd — AMD APU / UMA GPU residency watcher plugin for mgd (scaffold).
//!
//! Mirrors the Intel plugin but targets AMD APU iGPU fdinfo accounting.
//! Not yet implemented.
fn main() {
    todo!("mgd-gpu-amd: AMD APU UMA watcher — scaffold only")
}
